@model SalaryRequestModel
@{
    ViewData["Title"] = User.IsInRole("Admin") ? "Расчёт зарплаты" : "Моя зарплата";
}

<div class="row justify-content-center">
    <div class="col-md-7">
        <h2 class="mb-4 fs-4"><i class="bi bi-calculator me-2 text-primary"></i>@ViewData["Title"]</h2>

        <div class="card">
            <div class="card-body">
                <form asp-action="Calculate" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    @if (User.IsInRole("Admin"))
                    {
                        <div class="mb-3">
                            <label asp-for="EmployeeId" class="form-label fw-semibold">Сотрудник</label>
                            <select asp-for="EmployeeId" class="form-select" asp-items="ViewBag.Employees">
                                <option value="0">— Выберите сотрудника —</option>
                            </select>
                        </div>
                    }
                    else
                    {
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Сотрудник</label>
                            <p class="fw-bold fs-5">@ViewBag.EmployeeName</p>
                            <input asp-for="EmployeeId" type="hidden" />
                        </div>
                    }

                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <label asp-for="DateFrom" class="form-label fw-semibold">Период с</label>
                            <input asp-for="DateFrom" type="date" class="form-control" />
                            <span asp-validation-for="DateFrom" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="DateTo" class="form-label fw-semibold">по</label>
                            <input asp-for="DateTo" type="date" class="form-control" />
                            <span asp-validation-for="DateTo" class="text-danger small"></span>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary btn-lg w-100">
                        <i class="bi bi-calculator me-2"></i>Рассчитать
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
